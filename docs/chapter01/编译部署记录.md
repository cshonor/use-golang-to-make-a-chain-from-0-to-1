# 比原链编译部署记录

## 📋 环境信息

- **操作系统**: Windows 10
- **Go 版本**: go1.24.1 windows/amd64
- **GOPATH**: C:\Users\12392\go
- **项目路径**: C:\Users\12392\Desktop\golang\golangchain\bytom

---

## 📥 源码下载

### 1. 克隆仓库

```bash
# 克隆到 GOPATH（用于编译）
git clone https://github.com/Bytom/bytom.git C:\Users\12392\go\src\github.com\bytom

# 克隆到学习项目目录（用于参考）
git clone https://github.com/Bytom/bytom.git C:\Users\12392\Desktop\golang\golangchain\bytom
```

### 2. 切换到指定版本

```bash
cd C:\Users\12392\go\src\github.com\bytom
git fetch origin v1.0.5
git checkout v1.0.5
```

**版本信息：**
- **版本标签**: v1.0.5
- **提交哈希**: 2bc2396a
- **提交信息**: Merge pull request #1300 from Bytom/dev

---

## 🔨 编译过程

### 编译命令

由于 Windows 系统没有 `make` 命令，直接使用 `go build` 编译：

```bash
cd C:\Users\12392\Desktop\golang\golangchain\bytom

# 编译 bytomd
$commit = git rev-parse HEAD
go build -ldflags "-X github.com/bytom/bytom/version.GitCommit=$commit" -o cmd/bytomd/bytomd.exe cmd/bytomd/main.go

# 编译 bytomcli
go build -ldflags "-X github.com/bytom/bytom/version.GitCommit=$commit" -o cmd/bytomcli/bytomcli.exe cmd/bytomcli/main.go
```

### 编译结果

✅ **编译成功**

**生成的可执行文件：**
- `cmd/bytomd/bytomd.exe` - 比原链主节点程序
- `cmd/bytomcli/bytomcli.exe` - 比原链命令行客户端

**验证编译结果：**
```bash
# 查看 bytomd 帮助信息
.\cmd\bytomd\bytomd.exe --help

# 查看 bytomcli 帮助信息
.\cmd\bytomcli\bytomcli.exe --help
```

---

## 🚀 初始化节点

### 初始化命令

```bash
cd C:\Users\12392\Desktop\golang\golangchain\bytom
.\cmd\bytomd\bytomd.exe init --chain_id solonet
```

### 初始化结果

✅ **初始化成功**

**输出信息：**
```
time="Feb 12 19:51:41.903" level=info msg="success generate private" pubkey=a9d85661d4bc7344ad90469c75586458e6c90915e3c00fdf44c9da03c1cfc14abbfc31e7fb500b802171300d5a079a766cc7f855ea1d69f03c569c76b9ce5643
time="Feb 12 19:51:41.911" level=info msg="Initialized bytom" config="C:\\Users\\12392\\AppData\\Roaming\\Bytom2/config.toml" module=cmd
```

**生成的文件：**
- **配置文件**: `C:\Users\12392\AppData\Roaming\Bytom2\config.toml`
- **数据目录**: `C:\Users\12392\AppData\Roaming\Bytom2\data`

### 链类型说明

比原链支持三种链类型：

| 链类型 | 说明 | 用途 |
|--------|------|------|
| `mainnet` | 主网 | 连接到比原链主网 |
| `testnet` | 测试网 | 连接到测试网 wisdom |
| `solonet` | 独立模式 | 本地独立运行，用于开发和测试 |

**本次初始化使用**: `solonet`（独立模式，适合学习开发）

---

## 📁 目录结构

### 编译后的目录结构

```
bytom/
├── cmd/
│   ├── bytomd/
│   │   └── bytomd.exe          # 主节点可执行文件
│   └── bytomcli/
│       └── bytomcli.exe        # 命令行客户端可执行文件
├── go.mod                      # Go 模块定义
├── go.sum                      # 依赖校验和
└── ...                         # 源码文件
```

### 数据目录结构

```
C:\Users\12392\AppData\Roaming\Bytom2\
├── config.toml                 # 配置文件
└── data/                       # 数据目录
    ├── *.db                    # LevelDB 数据库文件
    └── ...                     # 其他数据文件
```

---

## 🔧 常用命令

### bytomd 命令

```bash
# 初始化节点
.\cmd\bytomd\bytomd.exe init --chain_id solonet

# 运行节点
.\cmd\bytomd\bytomd.exe node

# 查看版本
.\cmd\bytomd\bytomd.exe version

# 查看帮助
.\cmd\bytomd\bytomd.exe --help
```

### bytomcli 命令

```bash
# 创建密钥
.\cmd\bytomcli\bytomcli.exe create-key

# 创建账户
.\cmd\bytomcli\bytomcli.exe create-account

# 创建资产
.\cmd\bytomcli\bytomcli.exe create-asset

# 查看账户余额
.\cmd\bytomcli\bytomcli.exe list-balances

# 查看帮助
.\cmd\bytomcli\bytomcli.exe --help
```

---

## 📝 注意事项

1. **编译环境**：
   - Windows 系统需要使用 PowerShell 执行命令
   - 需要 Go 1.16 或更高版本
   - 编译前确保已下载所有依赖

2. **配置文件位置**：
   - Windows: `%APPDATA%\Bytom2\config.toml`
   - 默认路径: `C:\Users\<用户名>\AppData\Roaming\Bytom2\config.toml`

3. **数据存储**：
   - 所有链上数据存储在 `data/` 目录
   - 使用 LevelDB 作为持久化存储
   - 删除 `data/` 目录可以重置链状态

4. **网络模式**：
   - `solonet`: 独立运行，不连接其他节点
   - `testnet`: 连接到测试网络
   - `mainnet`: 连接到主网络

---

## ✅ 完成状态

- [x] 下载源码（v1.0.5）
- [x] 编译 bytomd
- [x] 编译 bytomcli
- [x] 初始化节点（solonet）
- [ ] 运行节点
- [ ] 创建账户和资产
- [ ] 发送交易测试

---

## 🔗 参考资源

- [比原链 GitHub](https://github.com/Bytom/bytom)
- [比原链文档](https://github.com/Bytom/wiki)
- 《Go 语言公链开发实战》第一章

---

**记录时间**: 2026-02-12  
**记录人**: 学习笔记

